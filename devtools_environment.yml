channels:
  - defaults
dependencies:
  - graphviz
  - python-graphviz
  - black >=25
  - make
  - conda-forge::pygal  # for pytest-benchmark to render SVG histograms
  - jupyter
  - notebook
  - pip
  - pip:
    - numba-scfg 
    - egglog ==10
    - pytest-benchmark >=5.2.0
