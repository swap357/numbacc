name: numbacc

environments:
  default:
    - environment.yml
  dev:
    - py312_environment.yml
    - mlir21_environment.yml
    - devtools_environment.yml

commands:
  build:
    cmd: make build
    environment: dev
  dev-run:
    cmd: zsh
    environment: dev
  fmt:
    cmd: black nbcc
    environment: dev
